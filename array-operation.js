function sumOfNumber(numbers){
    let sum = 0;
    for(number of numbers){
        console.log(number)
        sum = sum + number;
    }
    return sum;
}

let numbs = [24,51,62,34]
let sum = sumOfNumber(numbs);
console.log("sum of the number is: ", sum);